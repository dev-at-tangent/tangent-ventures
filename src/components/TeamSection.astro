---
import TeamMemberCard from "./TeamMemberCard";
---

<div class="flex flex-col items-center gap-y-12">
  <h1 class="text-5xl mt-16  font-medium">MEET OUR TEAM</h1>
  <span class="w-2/5 text-center text-grey-80"
    >We are a collective of experienced operators and founders behind some of
    the largest projects and protocols in Web3.</span
  >
  <div class="grid grid-cols-2 gap-12">
    <TeamMemberCard isHighlighted={true} client:only="react" />
    <TeamMemberCard isHighlighted={true} client:only="react" />
  </div>
  <div class="grid grid-cols-3 gap-12 w-11/12 ">
    <TeamMemberCard isHighlighted={false} client:only="react" />
    <TeamMemberCard isHighlighted={false} client:only="react" />
    <TeamMemberCard isHighlighted={false} client:only="react" />
    <TeamMemberCard isHighlighted={false} client:only="react" />
  </div>
</div>
